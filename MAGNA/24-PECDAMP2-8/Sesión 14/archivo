{"nbformat":4,"nbformat_minor":0,"metadata":{"colab":{"provenance":[]},"kernelspec":{"name":"python3","display_name":"Python 3"},"language_info":{"name":"python"}},"cells":[{"cell_type":"code","execution_count":null,"metadata":{"colab":{"base_uri":"https://localhost:8080/"},"id":"_q1Vbz-fdldw","executionInfo":{"status":"ok","timestamp":1734395881838,"user_tz":300,"elapsed":664,"user":{"displayName":"MARIO ANDERSON GUTIERREZ TERRONES","userId":"12277729976661677662"}},"outputId":"ff1d58f2-bf4d-4f87-f05f-5233641d7357"},"outputs":[{"output_type":"stream","name":"stdout","text":["Archivo Excel 'modulo07_dataset_mantenimiento.xlsx' creado exitosamente.\n"]}],"source":["import numpy as np\n","import pandas as pd\n","import random\n","from datetime import datetime, timedelta\n","\n","# Paso 1: Crear un conjunto de datos simulado de fallas\n","np.random.seed(44)  # Para reproducibilidad\n","n_samples = 2000\n","\n","# Generación de datos simulados\n","equipos = [f\"Equipo_{i}\" for i in range(1, 11)]  # 10 equipos diferentes\n","tipos_mantenimiento = [\"Correctivo-Programado\", \"Preventivo\", \"Correctivo\"]\n","\n","# Simulando las fechas de fallas\n","fechas_falla = [datetime(2020, 1, 1) + timedelta(days=random.randint(0, 1000)) for _ in range(n_samples)]\n","\n","# Simulando horas de uso (entre 100 y 5000 horas)\n","horas_uso = np.random.uniform(100, 5000, n_samples)\n","horas_uso = np.round(horas_uso, 1)\n","\n","# Simulando costos de reparación (entre 100 y 5000)\n","costos_reparacion = np.random.uniform(100, 5000, n_samples)\n","costos_reparacion = np.round(costos_reparacion, 2)\n","\n","# Asignando tipo de mantenimiento\n","tipos_mantenimiento = [random.choice(tipos_mantenimiento) for _ in range(n_samples)]\n","\n","# Selección de equipo para cada falla\n","equipos_falla = [random.choice(equipos) for _ in range(n_samples)]\n","\n","# Crear un DataFrame con los datos simulados\n","data = pd.DataFrame({\n","    'Equipo': equipos_falla,\n","    'Fecha de Falla': fechas_falla,\n","    'Horas de Uso': horas_uso,\n","    'Costo de Reparación': costos_reparacion,\n","    'Tipo de Mantenimiento': tipos_mantenimiento\n","})\n","\n","# Guardar los datos en un archivo Excel\n","data.to_excel('modulo07_dataset_mantenimiento.xlsx', index=False)\n","\n","print(\"Archivo Excel 'modulo07_dataset_mantenimiento.xlsx' creado exitosamente.\")"]},{"cell_type":"code","source":["import numpy as np\n","import pandas as pd\n","import random\n","from datetime import datetime, timedelta\n","\n","# Paso 1: Crear un conjunto de datos simulado de fallas\n","np.random.seed(44)  # Para reproducibilidad\n","n_samples = 2000\n","\n","# Generación de datos simulados\n","equipos = [f\"Equipo_{i}\" for i in range(1, 11)]  # 10 equipos diferentes\n","tipos_mantenimiento = [\"Correctivo-Programado\", \"Preventivo\", \"Correctivo\"]\n","\n","# Simulando las fechas de fallas\n","fechas_falla = [datetime(2020, 1, 1) + timedelta(days=random.randint(0, 1000)) for _ in range(n_samples)]\n","\n","# Simulando horas de uso (entre 100 y 5000 horas)\n","horas_uso = np.random.uniform(100, 5000, n_samples)\n","horas_uso = np.round(horas_uso, 1)\n","\n","# Simulando costos de reparación (entre 100 y 5000)\n","costos_reparacion = np.random.uniform(100, 5000, n_samples)\n","costos_reparacion = np.round(costos_reparacion, 2)\n","\n","# Asignando tipo de mantenimiento\n","tipos_mantenimiento = [random.choice(tipos_mantenimiento) for _ in range(n_samples)]\n","\n","# Selección de equipo para cada falla\n","equipos_falla = [random.choice(equipos) for _ in range(n_samples)]\n","\n","# Crear un DataFrame con los datos simulados\n","data = pd.DataFrame({\n","    'Equipo': equipos_falla,\n","    'Fecha de Falla': fechas_falla,\n","    'Horas de Uso': horas_uso,\n","    'Costo de Reparación': costos_reparacion,\n","    'Tipo de Mantenimiento': tipos_mantenimiento\n","})\n","\n","# Guardar los datos en un archivo Excel\n","nombre_archivo = 'modulo07_mantenimiento_2024.xlsx'\n","data.to_excel(nombre_archivo, index=False)\n","\n","print(f\"Archivo Excel '{nombre_archivo}' creado exitosamente.\")"],"metadata":{"colab":{"base_uri":"https://localhost:8080/"},"id":"Azk8kUWCW3vn","executionInfo":{"status":"ok","timestamp":1734395804689,"user_tz":300,"elapsed":745,"user":{"displayName":"MARIO ANDERSON GUTIERREZ TERRONES","userId":"12277729976661677662"}},"outputId":"c029cfbd-c391-4450-c631-629f12048979"},"execution_count":null,"outputs":[{"output_type":"stream","name":"stdout","text":["Archivo Excel 'modulo07_mantenimiento_2024.xlsx' creado exitosamente.\n"]}]},{"cell_type":"code","source":["# Clase 2\n","import pandas as pd\n","import random\n","from datetime import datetime, timedelta\n","\n","# Crear datos aleatorios\n","equipos = [\"Compresor\", \"Turbina\", \"Motor\", \"Bomba\"]\n","\n","# Generar una lista de fechas aleatorias entre el 1 de enero de 2024 y el 31 de diciembre de 2024\n","start_date = datetime(2024, 1, 1)\n","end_date = datetime(2024, 12, 31)\n","date_range = [start_date + timedelta(days=random.randint(0, (end_date - start_date).days)) for _ in range(200)]\n","\n","# Crear el DataFrame con los datos\n","data = pd.DataFrame({\n","    \"Equipo\": [random.choice(equipos) for _ in range(200)],\n","    \"Fecha_de_Falla\": date_range,\n","    \"Costo_de_Reparacion\": [random.uniform(500, 5000) for _ in range(200)],\n","    \"Horas_de_Operacion\": [random.uniform(1000, 10000) for _ in range(200)],\n","})\n","\n","# Redondear los valores a los decimales deseados\n","data['Costo_de_Reparacion'] = data['Costo_de_Reparacion'].round(2)  # Redondear a 2 decimales\n","data['Horas_de_Operacion'] = data['Horas_de_Operacion'].round(1)  # Redondear a 1 decimal\n","\n","# Guardar los datos en un archivo Excel\n","nombre_archivo = 'modulo07_dataset_R.xlsx'\n","data.to_excel(nombre_archivo, index=False)\n","\n","print(f\"Archivo Excel '{nombre_archivo}' creado exitosamente.\")"],"metadata":{"colab":{"base_uri":"https://localhost:8080/"},"id":"iZngn10P63Kg","executionInfo":{"status":"ok","timestamp":1734578606442,"user_tz":300,"elapsed":372,"user":{"displayName":"MARIO ANDERSON GUTIERREZ TERRONES","userId":"12277729976661677662"}},"outputId":"d3cbc15b-9ccc-4260-f262-c54b2cf0d9e2"},"execution_count":null,"outputs":[{"output_type":"stream","name":"stdout","text":["Archivo Excel 'modulo07_dataset_R.xlsx' creado exitosamente.\n"]}]},{"cell_type":"code","source":["import pandas as pd\n","import numpy as np\n","from datetime import timedelta, datetime\n","\n","# Función para generar fechas aleatorias dentro de un rango\n","def generate_random_dates(start_date, end_date, n):\n","    start_timestamp = start_date.timestamp()\n","    end_timestamp = end_date.timestamp()\n","    random_timestamps = np.random.uniform(start_timestamp, end_timestamp, n)\n","    return [datetime.fromtimestamp(timestamp) for timestamp in random_timestamps]\n","\n","# Parámetros de simulación más realistas\n","np.random.seed(42)  # Semilla para reproducibilidad\n","n_fallas = 100  # Número de fallas simuladas\n","equipos = ['Equipo A', 'Equipo B', 'Equipo C', 'Equipo D']\n","\n","# Generación de las fechas de falla con un patrón secuencial\n","# La probabilidad de fallo depende de la fecha y el equipo (mayor en ciertos momentos)\n","\n","# Parametros de la distribución de fallas\n","mean_time_between_failures = 50  # Promedio de tiempo entre fallas (en horas)\n","scale = 50  # Escala para la distribución de fallas\n","\n","# Distribuir las fallas de manera que los equipos con más fallas tengan más probabilidad de fallar\n","probabilidades_equipos = np.array([0.4, 0.3, 0.2, 0.1])  # Probabilidades de que ciertos equipos fallen más\n","\n","# Inicializamos los datos\n","tiempo_entre_fallas = []\n","fechas_falla = []\n","equipos_fallidos = []\n","\n","# Generar fallas con patrones\n","for i in range(n_fallas):\n","    equipo = np.random.choice(equipos, p=probabilidades_equipos)  # Elegimos el equipo\n","    tiempo_anterior = tiempo_entre_fallas[-1] if tiempo_entre_fallas else 0\n","    # Generar el tiempo entre fallas con un patrón (si ha habido fallas recientes, generar una falla más pronta)\n","    tiempo_falla = np.random.exponential(scale=mean_time_between_failures)\n","    if tiempo_anterior > 0 and np.random.rand() < 0.3:  # Si ya hubo fallas, aumentar la frecuencia\n","        tiempo_falla /= 1.5\n","\n","    tiempo_entre_fallas.append(tiempo_falla)\n","\n","    # Generamos la fecha de la falla (simulando la secuencia)\n","    fecha_falla = fechas_falla[-1] + timedelta(hours=tiempo_falla) if fechas_falla else generate_random_dates(datetime(2020, 1, 1), datetime(2023, 12, 31), 1)[0]\n","    fechas_falla.append(fecha_falla)\n","\n","    # Asignamos el equipo y el costo de reparación\n","    equipos_fallidos.append(equipo)\n","\n","# Generar los costos de reparación relacionados con el tiempo entre fallas\n","costo_reparacion = np.array([np.random.uniform(100, 1000) * (np.log(t + 1) + 1) for t in tiempo_entre_fallas])\n","\n","# Crear el dataframe\n","data = pd.DataFrame({\n","    'Equipo': equipos_fallidos,\n","    'Fecha de Falla': fechas_falla,\n","    'Tiempo entre Fallas': tiempo_entre_fallas,\n","    'Costo de Reparación': costo_reparacion\n","})\n","\n","# Guardar los datos en un archivo Excel\n","nombre_archivo = 'modulo07_dataset_R_2.xlsx'\n","data.to_excel(nombre_archivo, index=False)\n","\n","print(f\"Archivo Excel '{nombre_archivo}' creado exitosamente.\")"],"metadata":{"colab":{"base_uri":"https://localhost:8080/"},"id":"FaQl7l4PZ8tw","executionInfo":{"status":"ok","timestamp":1734653614062,"user_tz":300,"elapsed":343,"user":{"displayName":"MARIO ANDERSON GUTIERREZ TERRONES","userId":"12277729976661677662"}},"outputId":"75c84bbe-5f41-40b7-b9a2-f2c27ead5bba"},"execution_count":null,"outputs":[{"output_type":"stream","name":"stdout","text":["Archivo Excel 'modulo07_dataset_R_2.xlsx' creado exitosamente.\n"]}]}]}